server:
  port: 8084

spring:
  application:
    name: venta-service
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/servicioTienda_ventas?serverTimezone=UTC
    username: root
    password: 

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

